[project]
name = "rerun-example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "rerun-sdk>=0.21.0"
]

[tool.uv]
dev-dependencies = [
  "mypy>=1.14.1",
  "ruff>=0.9.2",
  "types-requests>=2.32.0.20241016"
]
